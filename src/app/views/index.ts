import { view } from './lib';
import { 
    HOME_ROUTE,
    BUTTONS_ROUTE,
    CHECBOXES_ROUTE,
    routerAlias
} from './routes';


export {
    view,
    HOME_ROUTE,
    BUTTONS_ROUTE,
    CHECBOXES_ROUTE
} 

export default routerAlias;