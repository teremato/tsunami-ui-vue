import { view } from './lib';
import { 
    HOME_ROUTE,
    BUTTONS_ROUTE,
    CHECBOXES_ROUTE,
    CHECKBOX_GROUP_ROUTE,
    SELECT_ROUTE,
    routerAlias
} from './routes';


export {
    view,
    HOME_ROUTE,
    BUTTONS_ROUTE,
    CHECBOXES_ROUTE,
    CHECKBOX_GROUP_ROUTE,
    SELECT_ROUTE
} 

export default routerAlias;